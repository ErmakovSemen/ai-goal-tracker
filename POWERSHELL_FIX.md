# 🔧 Решение проблемы с активацией виртуального окружения в PowerShell

## ❌ Проблема

```
Невозможно загрузить файл ...\Activate.ps1, 
так как выполнение сценариев отключено в этой системе.
```

Это происходит из-за политики выполнения PowerShell (Execution Policy).

---

## ✅ Решение 1: Изменить политику выполнения (Рекомендуется)

### Вариант A: Для текущего пользователя (Безопасно)

Откройте PowerShell **от имени администратора** и выполните:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Затем попробуйте снова:
```powershell
.\venv\Scripts\Activate.ps1
```

### Вариант B: Для текущей сессии (Временно)

Если не хотите менять политику глобально:

```powershell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
.\venv\Scripts\Activate.ps1
```

---

## ✅ Решение 2: Использовать альтернативные команды активации

### Вариант A: Через python -m venv (если venv уже создан)

```powershell
# Вместо .\venv\Scripts\Activate.ps1 используйте:
python -m venv venv
venv\Scripts\python.exe -m pip install -r requirements.txt
```

### Вариант B: Прямой вызов Python из venv

```powershell
# Используйте полный путь к Python в venv
.\venv\Scripts\python.exe -m pip install -r requirements.txt
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload
```

---

## ✅ Решение 3: Использовать CMD вместо PowerShell

Откройте **Command Prompt** (CMD) вместо PowerShell:

```cmd
cd backend
venv\Scripts\activate.bat
pip install -r requirements.txt
uvicorn app.main:app --reload
```

CMD не имеет таких ограничений на выполнение скриптов.

---

## ✅ Решение 4: Использовать Git Bash (если установлен Git)

```bash
cd backend
source venv/Scripts/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

---

## 🎯 Быстрое решение (Скопируйте и выполните)

### В PowerShell (от имени администратора):

```powershell
# 1. Измените политику выполнения
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 2. Перейдите в папку backend
cd D:\Python_Project\ai-goal-tracker\ai-goal-tracker\backend

# 3. Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# 4. Установите зависимости
pip install -r requirements.txt

# 5. Запустите backend
uvicorn app.main:app --reload
```

---

## 🔍 Проверка текущей политики выполнения

Чтобы проверить текущую политику:

```powershell
Get-ExecutionPolicy -List
```

Вы увидите что-то вроде:
```
        Scope ExecutionPolicy
        ----- ---------------
MachinePolicy       Undefined
   UserPolicy       Undefined
      Process       Undefined
  CurrentUser       Undefined
 LocalMachine       Restricted
```

---

## 📝 Объяснение политик выполнения

- **Restricted** - Запрещено выполнение всех скриптов (по умолчанию)
- **RemoteSigned** - Разрешено выполнение локальных скриптов, удаленные должны быть подписаны (рекомендуется)
- **Unrestricted** - Разрешено выполнение всех скриптов (менее безопасно)
- **Bypass** - Обход всех проверок (только для тестирования)

---

## ⚠️ Безопасность

**RemoteSigned** - это безопасный вариант, который:
- ✅ Разрешает выполнение локальных скриптов (как Activate.ps1)
- ✅ Требует подписи для удаленных скриптов
- ✅ Не требует прав администратора для CurrentUser

---

## 🎯 Рекомендуемое решение

Используйте **Решение 1, Вариант A** - это самое правильное и безопасное решение:

```powershell
# Выполните один раз от имени администратора
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

После этого активация виртуального окружения будет работать всегда!

---

## ✅ Альтернатива: Использовать CMD

Если не хотите менять настройки PowerShell, просто используйте CMD:

```cmd
cd D:\Python_Project\ai-goal-tracker\ai-goal-tracker\backend
venv\Scripts\activate.bat
pip install -r requirements.txt
uvicorn app.main:app --reload
```

CMD не имеет таких ограничений и работает сразу!
