# 🚀 Настройка DeepSeek

DeepSeek — мощная и бесплатная модель для JSON-ответов. Отлично подходит для структурированных ответов.

## 📋 Быстрая настройка

### Шаг 1: Получи API ключ

1. Зайди на https://platform.deepseek.com
2. Зарегистрируйся (бесплатно)
3. Перейди в раздел API Keys
4. Создай новый ключ
5. Скопируй ключ

### Шаг 2: Настрой переменные окружения

В Render Dashboard для сервиса `ai-goal-tracker-api`:

```
LLM_PROVIDER=deepseek
LLM_API_KEY=sk-твой-ключ-здесь
LLM_MODEL=deepseek-chat
```

### Шаг 3: Перезапусти сервис

Render автоматически перезапустит сервис после изменения переменных окружения.

## 🎯 Доступные модели DeepSeek

- `deepseek-chat` — основная модель для чата (рекомендуется)
- `deepseek-coder` — специализированная модель для кода

## ✨ Преимущества DeepSeek

1. **Бесплатно** — есть бесплатный tier
2. **JSON Mode** — отличная поддержка структурированных ответов
3. **Быстро** — низкая задержка
4. **Качественно** — хорошее понимание инструкций

## 🔧 Настройка для JSON ответов

DeepSeek автоматически использует `response_format: {"type": "json_object"}` для лучшего парсинга JSON.

## 📊 Сравнение с другими провайдерами

| Провайдер | JSON Support | Скорость | Цена |
|-----------|--------------|----------|------|
| DeepSeek | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | Бесплатно |
| Groq | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Бесплатно |
| OpenAI | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | Платно |
| Ollama | ⭐⭐⭐ | ⭐⭐ | Бесплатно (локально) |

## 🐛 Решение проблем

### Ошибка: "API key not found"

**Решение:** Проверь, что `LLM_API_KEY` установлен в Render Dashboard.

### Ошибка: "401 Unauthorized"

**Возможные причины:**
1. Баланс аккаунта пустой (нужно пополнить для активации)
2. API ключ неправильный или истёк
3. Ключ не активирован

**Решение:**
1. Зайди на https://platform.deepseek.com
2. Проверь баланс аккаунта
3. Пополни баланс (даже минимальная сумма активирует API)
4. Проверь, что ключ активен в разделе API Keys
5. Создай новый ключ, если старый не работает

**Важно:** DeepSeek может требовать пополнения баланса для активации API, даже на бесплатном плане. Это стандартная практика для многих API сервисов.

### Ошибка: "Rate limit exceeded"

**Решение:** DeepSeek имеет лимиты на бесплатном плане. Подожди немного или перейди на платный план.

### JSON не парсится

**Решение:** DeepSeek должен возвращать валидный JSON. Если проблема остаётся, проверь логи в Render Dashboard.

## 📝 Пример использования

После настройки DeepSeek будет автоматически использоваться для всех AI-запросов в приложении.

Проверь работу:
1. Открой приложение
2. Создай цель
3. Начни чат с AI
4. AI должен отвечать в правильном JSON формате

