# Настройка Firebase для Push-уведомлений

## Шаг 1: Создание Firebase проекта

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **"Add project"** (или **"Создать проект"**)
3. Введите название проекта: `ai-goal-tracker` (или любое другое)
4. Отключите Google Analytics (опционально, для бесплатного плана можно оставить включенным)
5. Нажмите **"Create project"** (или **"Создать проект"**)

## Шаг 2: Добавление Android приложения

1. После создания проекта, нажмите на иконку **Android** (или **"Add app"** → **Android**)
2. Заполните форму:
   - **Android package name**: `com.yourcompany.aigoaltracker`
     - ⚠️ **Важно**: Это должно совпадать с `applicationId` в `frontend/android/app/build.gradle`
   - **App nickname** (опционально): `AI Goal Tracker`
   - **Debug signing certificate SHA-1** (опционально): для тестирования
3. Нажмите **"Register app"** (или **"Зарегистрировать приложение"**)

## Шаг 3: Скачивание google-services.json

1. После регистрации, Firebase предложит скачать `google-services.json`
2. **Скачайте файл**
3. Скопируйте его в `frontend/android/app/google-services.json`

## Шаг 4: Настройка Android проекта

### Проверьте build.gradle файлы:

**`frontend/android/build.gradle`** (корневой):
```gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
        classpath 'com.google.gms:google-services:4.4.0'  // ← Должна быть эта строка
    }
}
```

**`frontend/android/app/build.gradle`**:
```gradle
plugins {
    id 'com.android.application'
    id 'com.google.gms.google-services'  // ← Должна быть эта строка
}

android {
    defaultConfig {
        applicationId "com.yourcompany.aigoaltracker"  // ← Должен совпадать с Firebase
        // ...
    }
}

dependencies {
    // ...
    implementation 'com.google.firebase:firebase-messaging:23.0.0'
}
```

## Шаг 5: Получение FCM Server Key

### Вариант 1: Использование существующего ключа

1. В Firebase Console перейдите в **Project Settings** (⚙️)
2. Откройте вкладку **Cloud Messaging**
3. Найдите **"Server key"** (или **"Серверный ключ"**)
4. Скопируйте ключ - это ваш `FCM_SERVER_KEY`

### Вариант 2: Создание нового ключа (если нет)

1. В Firebase Console перейдите в **Project Settings** → **Cloud Messaging**
2. Если ключа нет, нажмите **"Cloud Messaging API (Legacy)"**
3. Включите API (если не включен)
4. Вернитесь в **Cloud Messaging** и найдите **"Server key"**

## Шаг 6: Добавление ключа в переменные окружения

### Для Render.com:

1. Перейдите в ваш Web Service в Render Dashboard
2. **Environment** → **Environment Variables**
3. Добавьте:
   ```
   FCM_SERVER_KEY=<ваш скопированный ключ>
   ```
4. Нажмите **"Save Changes"**
5. Render автоматически перезапустит сервис

### Для локальной разработки:

1. Создайте файл `backend/.env`
2. Добавьте:
   ```
   FCM_SERVER_KEY=<ваш скопированный ключ>
   ```

## Шаг 7: Проверка работы

1. Установите приложение на Android устройство
2. Откройте приложение и войдите
3. Приложение автоматически зарегистрирует токен устройства
4. Проверьте логи backend - должны появиться сообщения о регистрации токена

## 🔍 Проверка токенов в базе данных

Если хотите проверить, что токены регистрируются:

1. Подключитесь к вашей PostgreSQL базе данных
2. Выполните запрос:
   ```sql
   SELECT * FROM device_tokens WHERE user_id = 1;
   ```
3. Должны увидеть записи с токенами устройств

## 🐛 Решение проблем

### Токены не регистрируются

- Проверьте логи приложения: `adb logcat | grep -i push`
- Убедитесь, что разрешения на уведомления предоставлены
- Проверьте, что `google-services.json` находится в правильной директории

### Push-уведомления не приходят

- Проверьте `FCM_SERVER_KEY` в переменных окружения backend
- Убедитесь, что токен активен в базе данных
- Проверьте логи backend при отправке уведомления
- Убедитесь, что приложение не убито системой (для тестирования держите его открытым)

### Ошибка "MismatchSenderId"

- Убедитесь, что `google-services.json` соответствует вашему Firebase проекту
- Проверьте, что `applicationId` в `build.gradle` совпадает с package name в Firebase

## 📱 Тестирование push-уведомлений

### Через API:

```bash
curl -X POST https://your-api-url.onrender.com/api/push/test/ \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "title": "Тест",
    "body": "Это тестовое уведомление"
  }'
```

### Через создание цели с дедлайном:

1. Создайте цель в приложении
2. Установите дедлайн на ближайшее время
3. Дождитесь напоминания от proactive service

