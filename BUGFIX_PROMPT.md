# 🐛 Промпт для исправления ошибок AI Goal Tracker

## Обнаруженные проблемы

После тестирования приложения на https://ai-goal-tracker-web.onrender.com/ обнаружены следующие критические ошибки:

### 1. ❌ CORS ошибка (критично)
**Ошибка в консоли:**
```
Access to fetch at 'https://ai-goal-tracker-api.onrender.com/api/goals/?user_id=2' 
from origin 'https://ai-goal-tracker-web.onrender.com' has been blocked by CORS policy: 
No 'Access-Control-Allow-Origin' header is present on the requested resource.
```

**Причина:** 
В `backend/app/main.py` строка 22-25 неправильно обрабатывает CORS origins. Когда `CORS_ORIGINS=*`, код создает список `["*"]`, но FastAPI CORSMiddleware требует либо список конкретных доменов, либо специальную обработку для "*".

**Решение:**
Исправить логику CORS в `backend/app/main.py`:
```python
# Текущий код (строки 21-25):
cors_origins = os.getenv("CORS_ORIGINS", "*").split(",")
if cors_origins == ["*"]:
    # Allow all for mobile apps and development
    cors_origins = ["*"]

# Исправить на:
cors_origins_str = os.getenv("CORS_ORIGINS", "*")
if cors_origins_str == "*":
    cors_origins = ["*"]  # FastAPI принимает ["*"] для разрешения всех origins
else:
    cors_origins = [origin.strip() for origin in cors_origins_str.split(",")]
```

Также убедиться, что в Render Dashboard для сервиса `ai-goal-tracker-api` установлена переменная окружения:
- `CORS_ORIGINS=*` или `CORS_ORIGINS=https://ai-goal-tracker-web.onrender.com`

---

### 2. ❌ Ошибка подключения к базе данных PostgreSQL (критично)
**Ошибка:**
```
Error creating goal: (psycopg2.OperationalError) could not translate host name 
"dpg-d4r8f6re5dus73f4e6hg-a" to address: Name or service not known
```

**Причина:**
База данных на Render была пересоздана или удалена, и `DATABASE_URL` в переменных окружения устарел. Хост `dpg-d4r8f6re5dus73f4e6hg-a` больше не существует.

**Решение:**
1. Проверить в Render Dashboard, существует ли база данных `ai-goal-tracker-db`
2. Если база существует, скопировать актуальный `DATABASE_URL` из настроек базы данных
3. Обновить переменную окружения `DATABASE_URL` в сервисе `ai-goal-tracker-api` в Render Dashboard
4. Если база не существует, создать новую PostgreSQL базу данных в Render и подключить её к сервису через `render.yaml` или вручную в Dashboard

**Проверка в коде:**
В `backend/app/database/database.py` строка 9 уже правильно обрабатывает замену `postgres://` на `postgresql://`, но нужно убедиться, что URL валидный.

---

### 3. ⚠️ Ошибка 422 при запросе `/api/users/me`
**Ошибка:**
```
Failed to load resource: the server responded with a status of 422 () 
@ https://ai-goal-tracker-api.onrender.com/api/users/me:0
API Error: {detail: Array(1)}
```

**Причина:**
Возможные причины:
- JWT токен истек или невалиден
- Пользователь с таким username не существует в базе данных (из-за проблемы с БД)
- Проблема с валидацией токена в `get_current_user`

**Решение:**
1. После исправления проблемы с БД (пункт 2), эта ошибка должна исчезнуть
2. Добавить более детальное логирование ошибок в `backend/app/core/auth.py`:
```python
async def get_current_user(db: Session = Depends(get_db), token: str = Depends(oauth2_scheme)):
    credentials_exception = HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Could not validate credentials",
        headers={"WWW-Authenticate": "Bearer"},
    )
    try:
        payload = jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])
        username: str = payload.get("sub")
        if username is None:
            raise credentials_exception
    except JWTError as e:
        print(f"JWT Error: {e}")  # Добавить логирование
        raise credentials_exception
    user = crud.user.get_user_by_username(db, username=username)
    if user is None:
        print(f"User not found: {username}")  # Добавить логирование
        raise credentials_exception
    return user
```

3. Убедиться, что `SECRET_KEY` в Render Dashboard совпадает с тем, который использовался при создании токена

---

### 4. ⚠️ Проблема с обработкой ошибок на frontend
**Ошибка:**
```
Failed to load goals: Error: Failed to connect to API at 
https://ai-goal-tracker-api.onrender.com/api/goals/?user_id=2. 
Is the backend running at http://localhost:8000? Check CORS settings.
```

**Причина:**
Frontend показывает сообщение об ошибке, которое ссылается на `localhost:8000`, хотя используется production URL. Это вводит в заблуждение.

**Решение:**
Улучшить обработку ошибок в `frontend/src/services/api.ts` - убрать упоминание localhost из сообщений об ошибках в production.

---

## Приоритет исправлений

1. **Высокий приоритет:**
   - Исправить CORS (проблема #1) - блокирует все API запросы
   - Исправить подключение к БД (проблема #2) - блокирует создание целей и работу с данными

2. **Средний приоритет:**
   - Улучшить обработку ошибок 422 (проблема #3)
   - Улучшить сообщения об ошибках на frontend (проблема #4)

---

## Инструкции по исправлению

### Шаг 1: Исправить CORS
1. Открыть `backend/app/main.py`
2. Заменить строки 21-25 на исправленный код (см. выше)
3. Закоммитить и запушить изменения
4. Проверить, что в Render Dashboard для `ai-goal-tracker-api` установлена переменная `CORS_ORIGINS=*`

### Шаг 2: Исправить подключение к БД
1. Зайти в Render Dashboard → Databases
2. Проверить, существует ли база `ai-goal-tracker-db`
3. Если существует:
   - Открыть настройки базы данных
   - Скопировать `Internal Database URL` или `External Database URL`
   - Зайти в настройки сервиса `ai-goal-tracker-api` → Environment
   - Обновить переменную `DATABASE_URL` актуальным значением
   - Перезапустить сервис
4. Если не существует:
   - Создать новую PostgreSQL базу данных
   - Подключить её к сервису через `render.yaml` или вручную
   - Обновить `DATABASE_URL`

### Шаг 3: Проверить работу
1. После деплоя исправлений, открыть https://ai-goal-tracker-web.onrender.com/
2. Проверить консоль браузера - не должно быть CORS ошибок
3. Попробовать создать цель через AI
4. Проверить, что данные загружаются корректно

---

## Дополнительные рекомендации

1. **Мониторинг:** Добавить логирование всех ошибок подключения к БД для быстрой диагностики
2. **Health check:** Добавить endpoint `/health` для проверки состояния БД и API
3. **Обработка ошибок:** Улучшить обработку ошибок на frontend - показывать понятные сообщения пользователю
4. **Тестирование:** После исправлений протестировать все основные функции:
   - Регистрация/вход
   - Создание цели
   - Общение с ботом
   - Создание подцелей
   - Загрузка статистики

---

## Файлы для изменения

1. `backend/app/main.py` - исправить CORS
2. `backend/app/core/auth.py` - добавить логирование (опционально)
3. `frontend/src/services/api.ts` - улучшить сообщения об ошибках (опционально)
4. Render Dashboard - обновить переменные окружения

---

**Дата тестирования:** 2026-01-12  
**Версия frontend:** main.47d70081.js  
**URL тестирования:** https://ai-goal-tracker-web.onrender.com/

---

## ✅ Статус исправлений

### Исправлено в коде:
- ✅ **CORS** - Исправлена логика обработки CORS origins в `backend/app/main.py`
- ✅ **Логирование** - Добавлено детальное логирование ошибок в `backend/app/core/auth.py`
- ✅ **Сообщения об ошибках** - Улучшены сообщения об ошибках в `frontend/src/services/api.ts` (убрано упоминание localhost в production)
- ✅ **Health check** - Добавлен endpoint `/health` для проверки состояния API и БД

### Требует действий в Render Dashboard:
- ⚠️ **DATABASE_URL** - Необходимо обновить переменную окружения `DATABASE_URL` в Render Dashboard для сервиса `ai-goal-tracker-api`
- ⚠️ **CORS_ORIGINS** - Убедиться, что в Render Dashboard установлена переменная `CORS_ORIGINS=*` или `CORS_ORIGINS=https://ai-goal-tracker-web.onrender.com`

### Следующие шаги:
1. Закоммитить и запушить изменения в репозиторий
2. Дождаться автоматического деплоя на Render (или запустить вручную)
3. Обновить `DATABASE_URL` в Render Dashboard
4. Проверить работу приложения после деплоя
