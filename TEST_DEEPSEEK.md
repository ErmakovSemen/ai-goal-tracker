# 🧪 Как проверить DeepSeek на Render

## Способ 1: Через HTTP endpoint (самый простой)

После деплоя открой в браузере или через curl:

```bash
curl https://ai-goal-tracker-api.onrender.com/test-llm
```

Или просто открой в браузере:
```
https://ai-goal-tracker-api.onrender.com/test-llm
```

Этот endpoint покажет:
- Текущий провайдер (должен быть `deepseek`)
- Модель (должна быть `deepseek-chat`)
- Настроен ли API ключ
- Результат тестового запроса к DeepSeek

## Способ 2: Через SSH на Render

1. Зайди в Render Dashboard
2. Выбери сервис `ai-goal-tracker-api`
3. Нажми "Shell" (или "SSH")
4. Выполни:

```bash
cd /app
python3 test_deepseek.py
```

Скрипт проверит:
- ✅ Конфигурацию
- ✅ Подключение к DeepSeek API
- ✅ JSON формат ответов

## Способ 3: Через логи Render

1. Зайди в Render Dashboard
2. Выбери сервис `ai-goal-tracker-api`
3. Открой вкладку "Logs"
4. Отправь сообщение в приложении
5. Проверь логи на наличие:
   - `📋 Using JSON mode for DeepSeek` (если JSON mode включён)
   - Ошибок подключения к DeepSeek API

## Ожидаемый результат

Если DeepSeek настроен правильно, ты увидишь:

```json
{
  "provider": "deepseek",
  "model": "deepseek-chat",
  "api_key_set": true,
  "api_key_length": 51,
  "test_successful": true,
  "test_response": "Тест успешен! Я работаю правильно."
}
```

## Если что-то не так

### Провайдер не DeepSeek
**Проблема:** `"provider": "groq"` или другой

**Решение:** В Render Dashboard → Environment → установи `LLM_PROVIDER=deepseek`

### API ключ не установлен
**Проблема:** `"api_key_set": false`

**Решение:** В Render Dashboard → Environment → добавь `LLM_API_KEY=sk-твой-ключ`

### Тест не прошёл
**Проблема:** `"test_successful": false`

**Решение:** 
- Проверь, что API ключ правильный
- Проверь, что у тебя есть доступ к DeepSeek API
- Проверь логи в Render Dashboard на наличие ошибок

## Быстрая проверка через curl

```bash
# Проверить конфигурацию
curl https://ai-goal-tracker-api.onrender.com/test-llm | python3 -m json.tool

# Или просто
curl https://ai-goal-tracker-api.onrender.com/test-llm
```

