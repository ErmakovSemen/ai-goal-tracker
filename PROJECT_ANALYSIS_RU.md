# 📊 ПОЛНЫЙ ОТЧЕТ ПО ПРОЕКТУ AI GOAL TRACKER

## 🎯 ЧТО ЭТО ЗА ПРОЕКТ?

**AI Goal Tracker** — это приложение для отслеживания целей с AI-коучем, похожим на Duolingo. Приложение активно напоминает пользователю о целях, мотивирует и помогает их достигать через чат-интерфейс.

### Основные возможности:
- 🤖 **Проактивный AI-коуч** — AI сам пишет пользователю, напоминает о дедлайнах, мотивирует
- 💬 **Чат-формализация целей** — создание целей через естественный диалог с AI
- 📊 **Отслеживание прогресса** — вехи с дедлайнами и чек-листы
- ✅ **Чек-листы** — AI создает чек-листы для проверки прогресса
- 📱 **Push-уведомления** — напоминания и мотивационные сообщения
- 📈 **Статистика и аналитика** — отслеживание прогресса во времени
- 📱 **Мобильное приложение** — нативное Android приложение (APK)

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА

### Тип архитектуры: **Full-Stack приложение (Backend + Frontend + Mobile)**

```
┌─────────────────────────────────────────────────────────┐
│                    FRONTEND (React)                     │
│  - React + TypeScript                                   │
│  - Capacitor для мобильного приложения                  │
│  - Компоненты, страницы, сервисы                        │
└──────────────────┬──────────────────────────────────────┘
                   │ HTTP/REST API
┌──────────────────▼──────────────────────────────────────┐
│                 BACKEND (FastAPI)                       │
│  - Python/FastAPI                                       │
│  - PostgreSQL база данных                               │
│  - JWT аутентификация                                   │
│  - LLM интеграция (множество провайдеров)               │
│  - Проактивный сервис (напоминания)                     │
│  - Push-уведомления (Firebase)                          │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│              DATABASE (PostgreSQL)                       │
│  - Users, Goals, Milestones, Tasks                      │
│  - Chats, Messages, Reports, Agreements                 │
└─────────────────────────────────────────────────────────┘
```

---

## 📁 СТРУКТУРА ПРОЕКТА

### Backend (`/backend`)

```
backend/
├── app/
│   ├── api/              # API endpoints (роутеры)
│   │   ├── api.py        # Главный роутер, объединяет все endpoints
│   │   ├── users.py      # API для пользователей
│   │   ├── goals.py      # API для целей
│   │   ├── milestones.py # API для вех
│   │   ├── tasks.py      # API для задач
│   │   ├── chats.py      # API для чатов
│   │   ├── reports.py    # API для отчетов
│   │   ├── ai.py         # API для AI-взаимодействия
│   │   └── push.py       # API для push-уведомлений
│   │
│   ├── core/             # Ядро приложения
│   │   ├── config.py     # Настройки (переменные окружения)
│   │   ├── auth.py        # Аутентификация пользователей
│   │   └── security.py   # JWT токены, хеширование паролей
│   │
│   ├── crud/             # CRUD операции с БД
│   │   ├── crud_user.py
│   │   ├── crud_goal.py
│   │   ├── crud_milestone.py
│   │   ├── crud_task.py
│   │   ├── crud_chat.py
│   │   ├── crud_report.py
│   │   ├── crud_agreement.py
│   │   └── crud_device_token.py
│   │
│   ├── database/         # Подключение к БД
│   │   └── database.py   # SQLAlchemy engine, сессии
│   │
│   ├── models/           # SQLAlchemy модели (таблицы БД)
│   │   ├── user.py       # Модель пользователя
│   │   ├── goal.py       # Модель цели
│   │   ├── milestone.py  # Модель вехи
│   │   ├── task.py       # Модель задачи
│   │   ├── chat.py       # Модель чата
│   │   ├── message.py    # Модель сообщения
│   │   ├── report.py     # Модель отчета
│   │   ├── agreement.py  # Модель соглашения (дедлайны)
│   │   └── device_token.py # Токены для push-уведомлений
│   │
│   ├── schemas/          # Pydantic схемы (валидация данных)
│   │   ├── user.py       # Схемы для пользователей
│   │   ├── goal.py       # Схемы для целей
│   │   ├── milestone.py  # Схемы для вех
│   │   ├── task.py       # Схемы для задач
│   │   ├── chat.py       # Схемы для чатов
│   │   ├── message.py    # Схемы для сообщений
│   │   ├── report.py     # Схемы для отчетов
│   │   ├── agreement.py  # Схемы для соглашений
│   │   └── device_token.py
│   │
│   ├── services/         # Бизнес-логика
│   │   ├── llm_service.py      # Сервис для работы с LLM
│   │   ├── proactive_service.py # Проактивные напоминания
│   │   └── push_service.py     # Push-уведомления
│   │
│   └── main.py          # Точка входа FastAPI приложения
│
├── Dockerfile           # Docker образ для бэкенда
├── requirements.txt     # Python зависимости
└── env.example.txt      # Пример переменных окружения
```

### Frontend (`/frontend`)

```
frontend/
├── src/
│   ├── components/      # React компоненты
│   │   ├── GoalList.tsx        # Список целей
│   │   ├── GoalCard.tsx        # Карточка цели
│   │   ├── ChatInterface.tsx   # Интерфейс чата
│   │   ├── Mascot.tsx          # Маскот (анимация)
│   │   ├── ProgressBar.tsx     # Прогресс-бар
│   │   ├── ChecklistComponent.tsx # Чек-листы
│   │   ├── QuickGoalModal.tsx  # Быстрое создание цели
│   │   ├── BottomNavigation.tsx # Нижняя навигация
│   │   └── ...
│   │
│   ├── pages/          # Страницы приложения
│   │   ├── Home.tsx           # Главная страница
│   │   ├── ChatView.tsx       # Страница чата
│   │   ├── CreateGoal.tsx     # Создание цели
│   │   ├── Dashboard.tsx      # Дашборд
│   │   ├── Profile.tsx        # Профиль пользователя
│   │   └── Statistics.tsx    # Статистика
│   │
│   ├── services/       # Сервисы для API
│   │   ├── api.ts            # Основной API клиент
│   │   └── pushNotifications.ts # Push-уведомления
│   │
│   ├── config/         # Конфигурация
│   │   └── api.ts      # URL API
│   │
│   ├── App.tsx         # Главный компонент
│   └── index.tsx       # Точка входа
│
├── public/             # Статические файлы
├── capacitor.config.json # Конфигурация Capacitor
├── package.json       # Node.js зависимости
└── Dockerfile         # Docker образ для фронтенда
```

---

## 🔧 ТЕХНОЛОГИИ И БИБЛИОТЕКИ

### Backend (Python)

#### Основные фреймворки:
- **FastAPI** (0.68+) — современный веб-фреймворк для создания API
- **SQLAlchemy** (1.4+) — ORM для работы с БД
- **Pydantic** (1.8+) — валидация данных и схемы
- **Uvicorn** (0.15+) — ASGI сервер для FastAPI

#### База данных:
- **PostgreSQL** — основная БД (через psycopg2-binary)
- **SQLite** — fallback для разработки

#### Аутентификация:
- **python-jose** (3.3+) — JWT токены
- **passlib** (1.7.4+) — хеширование паролей (bcrypt)

#### HTTP клиент:
- **httpx** (0.24+) — асинхронные HTTP запросы к LLM API

#### Push-уведомления:
- **firebase-admin** (6.0+) — Firebase Cloud Messaging

### Frontend (TypeScript/React)

#### Основные библиотеки:
- **React** (18.2+) — UI библиотека
- **TypeScript** (4.9+) — типизированный JavaScript
- **react-router-dom** (7.10+) — маршрутизация

#### Мобильное приложение:
- **@capacitor/core** (6.2+) — Capacitor framework
- **@capacitor/device** — информация об устройстве
- **@capacitor/push-notifications** — push-уведомления

#### UI/Анимации:
- **rive-react** (4.24+) — Rive анимации (маскот)

#### Сборка:
- **react-scripts** (5.0.1) — Create React App

### AI/LLM Провайдеры

Проект поддерживает **8 различных провайдеров LLM**:

1. **Ollama** (локальный) — бесплатный, работает локально
2. **Groq** — быстрый и бесплатный API
3. **DeepSeek** — мощный и бесплатный
4. **OpenAI** — GPT модели
5. **OpenRouter** — агрегатор моделей
6. **Together AI** — облачные модели
7. **Hugging Face** — Inference API
8. **GitHub Models** — модели через GitHub API

---

## 🗄️ СТРУКТУРА БАЗЫ ДАННЫХ

### Таблицы:

1. **users** — пользователи
   - id, username, email, hashed_password, created_at, updated_at

2. **goals** — цели
   - id, title, description, user_id, status, progress, frequency, start_date, end_date

3. **milestones** — вехи целей
   - id, goal_id, title, description, target_date, is_completed

4. **tasks** — задачи
   - id, goal_id, milestone_id, title, description, due_date, is_completed, priority

5. **chats** — чаты для целей
   - id, goal_id, created_at, updated_at

6. **messages** — сообщения в чатах
   - id, chat_id, sender (user/ai), content, created_at

7. **reports** — отчеты о прогрессе
   - id, goal_id, content, created_at

8. **agreements** — соглашения (дедлайны)
   - id, goal_id, description, deadline, status, reminder_sent, checklist_sent

9. **device_tokens** — токены для push-уведомлений
   - id, user_id, token, platform

### Связи (Relationships):
- User → Goals (один ко многим)
- Goal → Milestones, Tasks, Chats, Reports, Agreements (один ко многим)
- Chat → Messages (один ко многим)
- User → DeviceTokens (один ко многим)

---

## 🔄 ПРИНЦИП РАБОТЫ ПРИЛОЖЕНИЯ

### 1. Аутентификация
- Пользователь регистрируется/входит
- Backend создает JWT токен
- Токен сохраняется в localStorage
- Все запросы идут с заголовком `Authorization: Bearer <token>`

### 2. Создание цели
- Пользователь создает цель через чат или форму
- AI помогает формализовать цель через диалог
- Создается запись в БД (Goal)
- Автоматически создается Chat для этой цели

### 3. Чат с AI
- Пользователь пишет сообщение
- Сообщение сохраняется в БД (Message)
- Отправляется запрос к LLM сервису
- AI генерирует ответ на основе:
  - Контекста цели
  - Истории сообщений
  - Системного промпта
- Ответ сохраняется как сообщение от AI
- AI может создавать:
  - Чек-листы (CHECKLIST)
  - Предложения (SUGGESTIONS)
  - Действия (ACTIONS) — создание задач, вех, соглашений

### 4. Проактивный сервис (Duolingo-стиль)
Фоновый сервис (`proactive_service.py`) работает постоянно и:

- **Проверяет дедлайны** каждые 5 минут:
  - Напоминания за 24, 12, 6, 2 часа до дедлайна
  - Чек-листы в момент дедлайна
  
- **Проверяет пропущенные дни** каждые 30 минут:
  - Сообщения если пользователь не заходил 1, 2, 3, 7+ дней
  
- **Утренние мотивации** каждый час (7-10 утра):
  - Приветственные сообщения с мотивацией

- **Отмечает пропущенные соглашения**:
  - Если дедлайн прошел без ответа

### 5. Push-уведомления
- При регистрации устройства сохраняется токен
- Проактивный сервис отправляет push через Firebase
- Пользователь получает уведомления даже когда приложение закрыто

---

## 📝 ПРИНЦИПЫ НАПИСАНИЯ КОДА

### Backend (Python)

#### 1. **Слоистая архитектура (Layered Architecture)**
```
API Layer (api/) → Business Logic (services/) → Data Layer (crud/) → Database (models/)
```

- **API Layer** — обработка HTTP запросов, валидация входных данных
- **Services** — бизнес-логика (LLM, проактивные сообщения)
- **CRUD** — операции с БД (создание, чтение, обновление, удаление)
- **Models** — определение структуры таблиц БД

#### 2. **Dependency Injection**
- FastAPI использует `Depends()` для внедрения зависимостей
- Пример: `db: Session = Depends(get_db)` — автоматически создает сессию БД

#### 3. **Pydantic схемы**
- Все данные валидируются через Pydantic
- `schemas/` — схемы для валидации входных/выходных данных
- Автоматическая документация API (Swagger)

#### 4. **Асинхронность**
- Используется `async/await` для:
  - HTTP запросов к LLM API
  - Проактивного сервиса
  - Push-уведомлений

#### 5. **Конфигурация через переменные окружения**
- Все настройки в `core/config.py`
- Используется `pydantic-settings` для загрузки из `.env`

### Frontend (React/TypeScript)

#### 1. **Компонентная архитектура**
- Разделение на компоненты (`components/`) и страницы (`pages/`)
- Переиспользуемые компоненты

#### 2. **Управление состоянием**
- React Hooks (`useState`, `useEffect`)
- Локальное состояние компонентов
- API сервисы для работы с бэкендом

#### 3. **Типизация TypeScript**
- Интерфейсы для всех данных (`User`, `Goal`, `Message`, etc.)
- Типобезопасность на этапе разработки

#### 4. **Сервисный слой**
- `services/api.ts` — централизованный API клиент
- Обработка токенов, ошибок, запросов

---

## 📚 ЗАЧЕМ НУЖНЫ ФАЙЛЫ .md?

Все `.md` файлы — это **документация проекта**. Они объясняют:

### Основные файлы:
- **README.md** — главная документация, быстрый старт
- **DEPLOYMENT.md** — как развернуть проект
- **QUICK_START.md** — быстрый старт для разработки

### Настройка LLM:
- **LLM_PROVIDERS.md** — обзор всех провайдеров LLM
- **LLM_SETUP.md** — общая настройка LLM
- **OLLAMA_SETUP.md** — настройка Ollama (локальный)
- **DEEPSEEK_SETUP.md** — настройка DeepSeek
- **GROQ_SETUP.md** — настройка Groq
- **OPENROUTER_SETUP.md** — настройка OpenRouter
- **TOGETHER_AI_SETUP.md** — настройка Together AI
- **HUGGINGFACE_SETUP.md** — настройка Hugging Face
- **GITHUB_MODELS_SETUP.md** — настройка GitHub Models

### Другие настройки:
- **FIREBASE_SETUP.md** — настройка Firebase для push-уведомлений
- **PUSH_NOTIFICATIONS_SETUP.md** — технические детали push
- **AUTH_SETUP.md** — настройка аутентификации
- **APK_BUILD_INSTRUCTIONS.md** — как собрать APK

### Решение проблем:
- **TROUBLESHOOTING.md** — решение общих проблем
- **DEEPSEEK_TROUBLESHOOTING.md** — проблемы с DeepSeek
- **OPENROUTER_TROUBLESHOOTING.md** — проблемы с OpenRouter

### Дополнительно:
- **PROMPTS.md** — системные промпты для AI
- **FREE_LLM_OPTIONS.md** — бесплатные варианты LLM
- **SWITCHING_PROVIDERS.md** — как переключить провайдера
- **API_TESTING.md** — как тестировать API

**Зачем это нужно?**
- Проект сложный, много настроек
- Разные провайдеры LLM требуют разной настройки
- Документация помогает быстро разобраться
- Новые разработчики могут быстро начать работу

---

## 🚀 КАК ЗАПУСТИТЬ ПРОЕКТ

### Локальная разработка:

#### Backend:
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# Настроить .env файл (скопировать из env.example.txt)
# Установить PostgreSQL или использовать SQLite

uvicorn app.main:app --reload
```

#### Frontend:
```bash
cd frontend
npm install
npm start
```

### Docker:
```bash
docker-compose up
```

### Production:
- Backend деплоится на Render.com (см. `render.yaml`)
- Frontend деплоится на Netlify/Vercel
- APK собирается автоматически через GitHub Actions

---

## 🎨 ОСОБЕННОСТИ РЕАЛИЗАЦИИ

### 1. Проактивный AI (Duolingo-стиль)
- AI сам пишет пользователю, даже если тот не активен
- Множественные напоминания о дедлайнах
- "Стыдящие" сообщения за пропущенные дни
- Утренние мотивации

### 2. Гибкая интеграция LLM
- Поддержка 8 провайдеров
- Легкое переключение через переменные окружения
- Автоматическое определение формата ответа (JSON/текст)

### 3. Структурированные ответы AI
- AI может возвращать JSON с действиями:
  - Создание задач
  - Создание вех
  - Создание соглашений (дедлайнов)
  - Чек-листы
  - Предложения для быстрого ответа

### 4. Мобильное приложение
- Capacitor для нативной интеграции
- Push-уведомления на Android
- Офлайн работа (частично)

---

## 📊 СТАТИСТИКА ПРОЕКТА

- **Backend**: ~15 Python файлов, ~2000+ строк кода
- **Frontend**: ~20 React компонентов, ~3000+ строк кода
- **Документация**: ~30 MD файлов
- **База данных**: 9 таблиц
- **API endpoints**: ~30+ endpoints
- **LLM провайдеров**: 8

---

## 🔐 БЕЗОПАСНОСТЬ

- JWT токены для аутентификации
- Хеширование паролей (bcrypt)
- CORS настройки
- Валидация всех входных данных (Pydantic)
- SQL injection защита (SQLAlchemy ORM)

---

## 📱 МОБИЛЬНОЕ ПРИЛОЖЕНИЕ

- Собирается через Capacitor
- APK автоматически собирается при push в main
- Поддержка push-уведомлений
- Нативная интеграция с Android

---

## 🎯 ИТОГ

Это **полнофункциональное приложение** для отслеживания целей с AI-коучем, которое:
- Использует современный стек технологий
- Поддерживает множество LLM провайдеров
- Имеет проактивный AI (как Duolingo)
- Работает на веб и мобильных устройствах
- Хорошо документировано

Проект написан по принципам **чистой архитектуры**, разделен на слои, легко расширяется и поддерживается.
