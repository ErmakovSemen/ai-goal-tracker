# 🌐 Информация о внешнем доступе

## ✅ Постоянный деплой на Render.com

**Production URL:** `https://ai-goal-tracker-api.onrender.com`

### Статус
- ✅ **Backend API развёрнут на Render.com**
- ✅ **API активен и отвечает на запросы**
- ✅ **Frontend настроен на использование Render URL**

### Проверка работы API

```bash
# Проверка корневого endpoint
curl https://ai-goal-tracker-api.onrender.com/

# Должен вернуть: {"message":"AI Goal Tracker API"}
```

### Конфигурация Render

- **Service Name:** `ai-goal-tracker-api`
- **Runtime:** Docker (используется Dockerfile из backend/)
- **Region:** Virginia (US East)
- **Instance Type:** Free tier (0.1 CPU, 512 MB)
- **Health Check:** `/` (корневой endpoint)
- **Auto-Deploy:** Включён - автоматически деплоится при push в GitHub

### Environment Variables на Render

Следующие переменные окружения настроены в Render Dashboard:
- `DATABASE_URL` - автоматически из PostgreSQL базы данных
- `SECRET_KEY` - для JWT токенов
- `LLM_PROVIDER` - провайдер LLM (groq, openai, и т.д.)
- `LLM_API_KEY` - API ключ для LLM
- `LLM_MODEL` - модель LLM
- `FCM_SERVER_KEY` - для push-уведомлений (если настроено)
- `PORT` - порт (устанавливается автоматически Render)

### Frontend конфигурация

Frontend настроен на использование Render URL в `frontend/src/config/api.ts`:

```typescript
const API_URL = process.env.REACT_APP_API_URL || 'https://ai-goal-tracker-api.onrender.com';
```

Для пересборки frontend с новым API URL:
```bash
cd frontend
npm install
npm run build
```

### Важные замечания

⚠️ **Free tier ограничения:**
- Сервис "засыпает" после 15 минут неактивности
- Первый запрос после пробуждения может занять 30-60 секунд
- Для production рекомендуется использовать платный план

### Локальная разработка

Для локальной разработки backend всё ещё доступен на `http://localhost:8000`:

```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### Временный туннель (для тестирования)

Если нужно быстро протестировать локальный backend извне, можно использовать Cloudflare Tunnel:

```bash
# Установка (если ещё не установлен)
brew install cloudflare/cloudflare/cloudflared

# Запуск туннеля
cloudflared tunnel --url http://localhost:8000
```

⚠️ URL туннеля меняется при каждом перезапуске.

### Мониторинг и логи

Логи и мониторинг доступны в Render Dashboard:
- Перейдите на https://dashboard.render.com
- Выберите сервис `ai-goal-tracker-api`
- Просмотрите логи в реальном времени

### Обновление деплоя

Деплой происходит автоматически при push в `main` ветку GitHub репозитория.

Для ручного деплоя:
1. Перейдите в Render Dashboard
2. Выберите сервис `ai-goal-tracker-api`
3. Нажмите "Manual Deploy" → "Deploy latest commit"
