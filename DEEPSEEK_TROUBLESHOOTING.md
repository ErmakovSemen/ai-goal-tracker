# 🔧 Устранение проблем с DeepSeek API

## ✅ Проверка после установки нового ключа

После установки нового API ключа в Render:

1. **Подожди 2-3 минуты** - Render перезапускает сервис
2. **Проверь через тестовый endpoint:**
   ```bash
   curl https://ai-goal-tracker-api.onrender.com/test-llm
   ```

3. **Ожидаемый результат:**
   ```json
   {
     "provider": "deepseek",
     "model": "deepseek-chat",
     "api_key_set": true,
     "api_key_length": 51,
     "api_key_starts_with_sk": true,
     "test_successful": true,
     "test_response": "Тест успешен! Я работаю правильно."
   }
   ```

## 🔍 Проверка в Render Dashboard

1. Зайди в Render Dashboard → сервис `ai-goal-tracker-api`
2. Открой вкладку **Environment**
3. Проверь переменные:
   - `LLM_PROVIDER=deepseek`
   - `LLM_API_KEY=sk-f971845c69ef41f3abd0739d4269d58f` (или твой новый ключ)
   - `LLM_MODEL=deepseek-chat`

4. Убедись, что:
   - Нет пробелов в начале/конце ключа
   - Ключ скопирован полностью
   - Ключ начинается с `sk-`

## 📋 Проверка логов

1. В Render Dashboard → **Logs**
2. Отправь тестовое сообщение в приложении
3. Ищи в логах:
   - `🔑 Using DeepSeek API key: sk-f971845c...`
   - `📤 Sending request to DeepSeek API`
   - `📥 Response status: 200` (успех) или `401` (ошибка)

## ❌ Если всё ещё ошибка 401

### Шаг 1: Проверь баланс на DeepSeek
1. Зайди на https://platform.deepseek.com
2. Проверь баланс аккаунта
3. Убедись, что баланс пополнен и виден

### Шаг 2: Проверь статус ключа
1. В разделе API Keys проверь:
   - Ключ активен (не удалён)
   - Ключ создан после пополнения баланса
   - Ключ имеет правильные права доступа

### Шаг 3: Создай новый ключ
Если старый ключ не работает:
1. Удали старый ключ
2. Создай новый ключ
3. Обнови `LLM_API_KEY` в Render
4. Подожди 2-3 минуты

### Шаг 4: Проверь формат ключа
Ключ должен:
- Начинаться с `sk-`
- Иметь длину около 51 символа
- Не содержать пробелов или переносов строк

## ✅ Если всё работает

После успешной настройки:
1. Тестовый endpoint должен вернуть `test_successful: true`
2. В приложении AI должен отвечать в правильном JSON формате
3. В логах Render не должно быть ошибок 401

## 📞 Дополнительная помощь

Если проблема остаётся:
1. Проверь логи в Render Dashboard на наличие детальных ошибок
2. Проверь баланс и статус ключа на platform.deepseek.com
3. Попробуй создать новый ключ и обновить его в Render

