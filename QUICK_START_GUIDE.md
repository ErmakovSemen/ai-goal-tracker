# 🚀 Быстрый старт: Запуск в браузере и сборка APK

## 📱 Вариант 1: Запуск в браузере (для разработки и тестирования)

### Шаг 1: Запустите Backend

```bash
# Перейдите в папку backend
cd backend

# Создайте виртуальное окружение (если еще не создано)
python -m venv venv

# Активируйте виртуальное окружение
# Windows PowerShell:
.\venv\Scripts\Activate.ps1
# Windows CMD:
venv\Scripts\activate.bat

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env (скопируйте из env.example.txt)
# Минимальный .env:
# SECRET_KEY=test-key-123
# LLM_PROVIDER=ollama
# OLLAMA_URL=http://localhost:11434

# Запустите backend
uvicorn app.main:app --reload
```

Backend будет доступен на: **http://localhost:8000**

### Шаг 2: Запустите Frontend

Откройте **новый терминал**:

```bash
# Перейдите в папку frontend
cd frontend

# Установите зависимости (если еще не установлены)
npm install

# Создайте файл .env в папке frontend
# Создайте файл frontend/.env со следующим содержимым:
echo REACT_APP_API_URL=http://localhost:8000 > .env

# Запустите frontend
npm start
```

Frontend автоматически откроется в браузере на: **http://localhost:3000**

### ✅ Готово!

Теперь вы можете:
- ✅ Использовать приложение в браузере
- ✅ Тестировать все функции
- ✅ Видеть изменения в реальном времени (hot reload)

---

## 📱 Вариант 2: Сборка APK для Android телефона

### Способ A: Автоматическая сборка через GitHub Actions (Рекомендуется)

#### Шаг 1: Настройте GitHub Secrets

1. Зайдите на GitHub → ваш репозиторий → **Settings** → **Secrets and variables** → **Actions**
2. Добавьте секрет:
   - **Name**: `REACT_APP_API_URL`
   - **Value**: `https://ai-goal-tracker-api.onrender.com` (или ваш URL backend)

#### Шаг 2: Запустите сборку

1. Сделайте любой commit и push в ветку `main`
2. GitHub Actions автоматически соберет APK
3. Зайдите в **Actions** → выберите последний workflow
4. Скачайте артефакт **android-apk**

#### Шаг 3: Установите APK на телефон

1. Перекиньте APK файл на Android телефон
2. Включите "Установку из неизвестных источников" в настройках
3. Откройте APK файл и установите

✅ **Готово!** Приложение установлено на телефон.

---

### Способ B: Локальная сборка APK (для разработки)

#### Требования:

- ✅ Node.js 18+ установлен
- ✅ Java JDK 17+ установлен
- ✅ Android Studio установлен
- ✅ Android SDK настроен

#### Шаг 1: Настройте Frontend

```bash
# Перейдите в папку frontend
cd frontend

# Установите зависимости
npm install

# Создайте .env файл
echo REACT_APP_API_URL=http://localhost:8000 > .env
# Или для продакшена:
# echo REACT_APP_API_URL=https://ai-goal-tracker-api.onrender.com > .env
```

#### Шаг 2: Соберите React приложение

```bash
# Соберите production версию
npm run build
```

Это создаст папку `build/` с готовым приложением.

#### Шаг 3: Установите Capacitor

```bash
# Установите Capacitor CLI глобально (если еще не установлен)
npm install -g @capacitor/cli

# Инициализируйте Capacitor (если еще не инициализирован)
npx cap init

# Добавьте Android платформу
npx cap add android

# Синхронизируйте файлы
npx cap sync android
```

#### Шаг 4: Настройте Android проект

```bash
# Откройте Android Studio
npx cap open android
```

В Android Studio:
1. Дождитесь синхронизации Gradle
2. Подключите Android телефон через USB (включите режим разработчика)
3. Или создайте эмулятор Android

#### Шаг 5: Соберите APK

**Вариант 1: Через Android Studio**
1. В Android Studio: **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. Дождитесь сборки
3. APK будет в: `frontend/android/app/build/outputs/apk/debug/app-debug.apk`

**Вариант 2: Через командную строку**
```bash
cd frontend/android
./gradlew assembleDebug
```

APK будет в: `frontend/android/app/build/outputs/apk/debug/app-debug.apk`

#### Шаг 6: Установите APK на телефон

**Через USB:**
```bash
# Подключите телефон через USB
adb install frontend/android/app/build/outputs/apk/debug/app-debug.apk
```

**Или вручную:**
1. Перекиньте APK файл на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников
4. Установите приложение

---

## 🌐 Вариант 3: Деплой в браузер для всех (Production)

### Способ 1: Netlify (Самый простой)

1. Зайдите на https://netlify.com
2. Войдите через GitHub
3. **Add new site** → **Import an existing project**
4. Выберите репозиторий `ai-goal-tracker`
5. Настройки:
   - **Base directory**: `frontend`
   - **Build command**: `npm ci && npm run build`
   - **Publish directory**: `frontend/build`
6. Добавьте переменную окружения:
   - **Key**: `REACT_APP_API_URL`
   - **Value**: `https://ai-goal-tracker-api.onrender.com`
7. Нажмите **Deploy site**

Через 2-3 минуты получите URL типа: `https://ai-goal-tracker.netlify.app`

### Способ 2: Vercel

1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. **Add New...** → **Project**
4. Выберите репозиторий `ai-goal-tracker`
5. Настройки:
   - **Framework Preset**: Create React App
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `build`
6. Добавьте переменную окружения:
   - **Key**: `REACT_APP_API_URL`
   - **Value**: `https://ai-goal-tracker-api.onrender.com`
7. Нажмите **Deploy**

Получите URL типа: `https://ai-goal-tracker.vercel.app`

---

## 🔧 Настройка API URL

### Для локальной разработки:
```env
# frontend/.env
REACT_APP_API_URL=http://localhost:8000
```

### Для продакшена:
```env
# frontend/.env
REACT_APP_API_URL=https://ai-goal-tracker-api.onrender.com
```

### Для APK (используйте продакшен URL):
```env
# frontend/.env
REACT_APP_API_URL=https://ai-goal-tracker-api.onrender.com
```

⚠️ **Важно**: После изменения `.env` пересоберите приложение:
```bash
npm run build
npx cap sync android  # Для APK
```

---

## 📋 Чек-лист для запуска в браузере

- [ ] Backend запущен на http://localhost:8000
- [ ] Frontend запущен на http://localhost:3000
- [ ] Файл `frontend/.env` создан с `REACT_APP_API_URL=http://localhost:8000`
- [ ] Приложение открывается в браузере
- [ ] Можно зарегистрироваться и войти
- [ ] API работает (проверьте http://localhost:8000/docs)

---

## 📋 Чек-лист для сборки APK

- [ ] Node.js 18+ установлен
- [ ] Java JDK 17+ установлен
- [ ] Android Studio установлен
- [ ] Android SDK настроен
- [ ] Frontend собран (`npm run build`)
- [ ] Capacitor установлен (`npx cap add android`)
- [ ] Android проект синхронизирован (`npx cap sync android`)
- [ ] APK собран успешно
- [ ] APK установлен на телефон

---

## 🐛 Решение проблем

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Проверьте, что backend запущен: http://localhost:8000
2. Проверьте файл `frontend/.env` - должен быть `REACT_APP_API_URL=http://localhost:8000`
3. Перезапустите frontend после изменения `.env`

### Проблема: Ошибки при сборке APK

**Решение:**
1. Убедитесь, что Android Studio установлен
2. Проверьте, что Android SDK настроен
3. Запустите `npx cap sync android` перед сборкой
4. Проверьте логи в Android Studio

### Проблема: APK не устанавливается на телефон

**Решение:**
1. Включите "Установку из неизвестных источников" в настройках Android
2. Убедитесь, что APK собран для правильной архитектуры (arm64-v8a)
3. Проверьте, что на телефоне достаточно места

---

## 🎯 Итоговые команды (быстрый старт)

### Запуск в браузере:
```bash
# Терминал 1: Backend
cd backend
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn app.main:app --reload

# Терминал 2: Frontend
cd frontend
npm install
echo REACT_APP_API_URL=http://localhost:8000 > .env
npm start
```

### Сборка APK:
```bash
cd frontend
npm run build
npx cap sync android
npx cap open android
# В Android Studio: Build → Build APK
```

---

## 📚 Дополнительная документация

- [LOCAL_TESTING_GUIDE.md](LOCAL_TESTING_GUIDE.md) - Подробная инструкция по локальному тестированию
- [APK_BUILD_INSTRUCTIONS.md](APK_BUILD_INSTRUCTIONS.md) - Детальная инструкция по сборке APK
- [DEPLOY_FRONTEND.md](DEPLOY_FRONTEND.md) - Инструкция по деплою в браузер

---

## ✅ Готово!

Теперь вы можете:
- ✅ Запускать приложение в браузере для разработки
- ✅ Собирать APK для Android телефона
- ✅ Деплоить приложение в интернет для всех

Удачи! 🚀
