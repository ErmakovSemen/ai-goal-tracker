# Скопируйте этот файл в .env и заполните значениями

# Database Configuration
# For Render.com, DATABASE_URL is automatically set
# For local development, use individual variables:
DATABASE_URL=postgresql://user:password@localhost:5432/ai_goal_tracker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ai_goal_tracker

# JWT Security
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# LLM Configuration
# Options: ollama, groq, openai, openrouter, together, huggingface, github
LLM_PROVIDER=groq
LLM_API_KEY=your-llm-api-key-here
LLM_MODEL=llama-3.1-8b-instant

# Ollama (if using local Ollama)
OLLAMA_URL=http://localhost:11434

# Firebase Cloud Messaging (FCM) for Push Notifications
FCM_SERVER_KEY=your-fcm-server-key-here

# CORS Origins (comma-separated, use * for all)
CORS_ORIGINS=*

# Telegram Bot (do not commit real token; keep real value only in server env)
TELEGRAM_BOT_TOKEN=

# Port (Render sets this automatically)
PORT=8000

